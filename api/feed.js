import { cors, requireToken, dbConfigured, select } from './_shared.js';
export default async function handler(req,res){cors(res);if(req.method==='OPTIONS')return res.status(204).end();if(req.method!=='GET')return res.status(405).json({error:'GET only'});const driverId=requireToken(req,res);if(!driverId)return;if(!dbConfigured())return res.status(200).json({offers:[],configured:false});const hours=Math.max(1,Math.min(168,Number(req.query?.hours||48))),since=new Date(Date.now()-hours*3600_000).toISOString();try{const offers=await select(`offers?driver_id=eq.${driverId}&captured_at=gte.${encodeURIComponent(since)}&order=captured_at.desc&limit=500&select=*`);return res.status(200).json({offers:offers||[],configured:true})}catch(error){return res.status(500).json({error:error.message})}}